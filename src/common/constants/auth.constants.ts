/** Metadata keys used by decorators/guards to attach data to route handlers. */
export const IS_PUBLIC_KEY = 'isPublic';
export const ROLES_KEY = 'roles';

/** Bearer token type returned in login/refresh responses. */
export const TOKEN_TYPE = 'Bearer';

/** Opts a route out of the global success-response envelope (e.g. health checks read by orchestrators). */
export const RAW_RESPONSE_KEY = 'rawResponse';
